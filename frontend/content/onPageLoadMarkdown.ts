export const onPageLoadMarkdown = `Learn what happens when the /are-you-ai/<session-id> page is loaded:

1. Request sent through the API layer to Workers KV with list of pre-generated AI Images
2. Random image url returned back from the frontend.  This is the image that the user will be asked to describe.
`
